mod problems101to110;
//mod problems111to120;
//mod problems121to130;
//mod problems131to140;
//mod problems141to150;
//mod problems151to160;
//mod problems161to170;
//mod problems171to180;
//mod problems181to190;
//mod problems191to200;

pub fn make_range() -> crate::ProblemList {
    // FromResidual is a nightly feature.
    problems101to110::make_range()
    //.join(problems111to120::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems121to130::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems131to140::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems141to150::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems151to160::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems161to170::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems171to180::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems181to190::make_range())
    //.expect("child lists have non-overlapping problem numbers")
    //.join(problems191to200::make_range())
    //.expect("child lists have non-overlapping problem numbers")
}
